 const divEl = document.getElementById('controls')
const inputEl = divEl.children
const createBtn = inputEl[1]
const quantityDivEl = inputEl[0]
createBtn.addEventListener('click', () => {
    const quantityEl = quantityDivEl.value
}) 
